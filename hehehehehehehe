
#영상녹화 코드 
gst-launch-1.0 ximagesrc ! video/x-raw, framerate=30/1 ! videoconvert ! omxh264enc ! qtmux ! filesink location=screen_record.mp4 -e


# 스크린샷 코드
gnome-screenshot -f my_screenshot.png
-------------------------------------------------------
from transformers import T5Tokenizer, T5ForConditionalGeneration
model_name = "flan_t5_local"
tokenizer = T5Tokenizer.from_pretrained(model_name)
model = T5ForConditionalGeneration.from_pretrained(model_name).to('cuda')
---------------------------------------------------------------------------
OSError                                   Traceback (most recent call last)
<ipython-input-2-cdc661bdd7a5> in <module>
      1 from transformers import T5Tokenizer, T5ForConditionalGeneration
      2 model_name = "flan_t5_local"
----> 3 tokenizer = T5Tokenizer.from_pretrained(model_name)
      4 model = T5ForConditionalGeneration.from_pretrained(model_name).to('cuda')

~/.local/lib/python3.6/site-packages/transformers/tokenization_utils_base.py in from_pretrained(cls, pretrained_model_name_or_path, *init_inputs, **kwargs)
   1763         if all(full_file_name is None for full_file_name in resolved_vocab_files.values()):
   1764             raise EnvironmentError(
-> 1765                 f"Can't load tokenizer for '{pretrained_model_name_or_path}'. If you were trying to load it from "
   1766                 "'https://huggingface.co/models', make sure you don't have a local directory with the same name. "
   1767                 f"Otherwise, make sure '{pretrained_model_name_or_path}' is the correct path to a directory "

OSError: Can't load tokenizer for 'flan_t5_local'. If you were trying to load it from 'https://huggingface.co/models', make sure you don't have a local directory with the same name. Otherwise, make sure 'flan_t5_local' is the correct path to a directory containing all relevant files for a T5Tokenizer tokenizer.
